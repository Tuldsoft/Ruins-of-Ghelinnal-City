using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// An enumeration of Keys used in PlayerPrefs, to prevent typos.
// Does not include key/value pairs that entitle save slots.
public enum PlayerPrefsKeys
{
    IsCheating,
    HighestSlotNum,
    SoundVolume,
    MusicVolume
}
